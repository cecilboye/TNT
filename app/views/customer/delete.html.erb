<%= link_to("<< Back to List", {:aciton => 'index'}, :class => 'back-link') %>

<div class="subjects delete">
	<h2>Delete Subject</h2>

	<%= form_for(:customer, :url => {:action => 'destroy', :id => @customer.id}) do |f| %>

	<p>Aare you sure you want to permanently delete this subject?</p>

	<p class ="reference-name"><%= @customername %></p>

	<div class="form-buttons">
		<%= submit_tag("Delete Subject") %>

	</div>

	<% end %>

 </div>